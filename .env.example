# API
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/xborg"
SESSION_SECRET="a-string-secret-at-least-256-bits-long"

# Public base URL of this API (no trailing slash). Align with NEXT_PUBLIC_API_URL host.
API_ORIGIN="http://localhost:3000"

# Full Google OAuth redirect URI (must match Google Console and the callback route on this API)
GOOGLE_CALLBACK_URL="http://localhost:3000/auth/validate/google"
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# Next.js origin (CORS + post-login redirect); no trailing slash
CLIENT_ORIGIN="http://localhost:4000"

# Web (NEXT_PUBLIC_* is exposed to the browser)
NEXT_PUBLIC_API_URL="http://localhost:3000"
# NEXT_PUBLIC_APP_URL="https://your-app.vercel.app"

# Docker (local Postgres)
POSTGRES_USER="postgres"
POSTGRES_PASSWORD="postgres"
POSTGRES_DB="xborg"
