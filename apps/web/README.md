# Web (`apps/web`)

Next.js App Router app (port **4000** in dev).

**Run from repo root** — see the [root README](../../README.md) (`pnpm dev`, env vars, `http://localhost:4000`).

Auth UX and `proxy.ts` are documented in [docs/auth-architecture.md](../../docs/auth-architecture.md).

This folder previously contained the default `create-next-app` README; behavior is defined in this monorepo, not the upstream template.
