# API (`apps/api`)

NestJS service: Google OAuth, `express-session`, user, feedback.

**Run from repo root** — see the [root README](../../README.md) (`pnpm dev`, env vars, `http://localhost:3000`).

This folder previously contained the default Nest CLI README; behavior is defined in this monorepo, not the upstream template.
